# Tests Directory

Future tests should cover:

```text
standard card package validation
import error handling
daily task generation
FSRS scheduling
ReviewEvent recording
UserMemoryState updates
word galaxy relation generation
```

Phase 0 only defines this test scope.
