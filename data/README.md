# Data Directory

This directory is for local test artifacts only.

```text
data/imports/   local card package import staging
data/exports/   local export staging
```

Do not treat files in this directory as the canonical database.
